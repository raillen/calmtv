# Debian's zram-generator consumes this file when installed.
# A quarter of RAM is a conservative default; hardware tuning stays explicit.
[zram0]
zram-size = ram / 4
compression-algorithm = zstd
swap-priority = 100
